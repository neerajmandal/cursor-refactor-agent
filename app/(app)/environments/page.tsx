export default function EnvironmentsPage() {
  return (
    <main className="px-8 py-10 md:px-12 md:py-12">
      <p className="text-[11px] font-medium uppercase tracking-[0.16em] text-muted">
        Environments
      </p>
      <h1 className="mt-3 font-serif text-4xl tracking-tight text-ink">
        Environments
      </h1>
      <p className="mt-4 max-w-lg text-[15px] leading-6 text-muted">
        Cursor cloud environments are managed in Cursor. On New migration you
        can name an existing environment, or leave it blank to clone the repo
        URLs.
      </p>
    </main>
  );
}
